<Window x:Class="TodoAgent.Learning.Wpf.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TodoAgent.Learning.Wpf"
        xmlns:vm="clr-namespace:TodoAgent.Learning.Wpf.ViewModels"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Window.DataContext>
        <vm:MainViewModel />
    </Window.DataContext>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel>
            <Button Name="btnTestConnect" Content="测试连接" Width="150" Height="90" Command="{Binding TestConnectCommand}" />
        </StackPanel>

        <ScrollViewer Grid.Row="1" Margin="0,10,0,0" >
            <TextBlock Text="{Binding ResponseText}" TextWrapping="Wrap" FontSize="14" />
        </ScrollViewer>
    </Grid>
</Window>
