# Issues - Agent Framework Learning Plan

## 2026-05-18

### CRITICAL: API Key Exposed in Git
- `appsettings.json` contains real DeepSeek API key
- Already committed in git history (commit 5510578)
- Status: PARTIALLY FIXED
- Done: .gitignore updated, appsettings.template.json created, git rm --cached executed
- Remaining: git history still contains the key (needs filter-branch or BFG cleanup)
- User should: regenerate the API key on DeepSeek console as ultimate safety measure

### BLOCKER: All Steps Awaiting User Code (PERSISTENT)
- User explicitly chose "自己写代码" mode — agent MUST NOT write code
- User said "下次再自己写代码，我就把你卸载了" (if you write code again, I'll uninstall you)
- Steps 1-6 were previously auto-generated by sub-agents and REVERTED per user request
- Code is now back to Step 0 (initial scaffold only)
- Guidance has been provided for Step 1 (6 tasks outlined in detail)
- Cannot proceed to Step 2 until Step 1 code is written and verified by user
- All subsequent steps depend on Step 1
- This is a learning project - user MUST write code, agent CANNOT skip ahead
- Updated 2026-05-18: Still awaiting user code
- BLOCKER IS PERMANENT until user writes code — agent cannot proceed on any task
- All 12 tasks depend on Step 1 being completed by the user first
- Agent has provided detailed Step 1 guidance (6 tasks with code patterns)
- No further action possible until user responds with code changes